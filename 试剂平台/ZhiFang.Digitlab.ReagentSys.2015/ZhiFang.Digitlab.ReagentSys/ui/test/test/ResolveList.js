/**
 * 应用程序解析
 * 测试版本
 */
var parser = {
	/**
	 * 
	 * @param {} DesignCode
	 * @return {}
	 */
	resolveList:function(DesignCode){
		var ClassCode = "";
		
		var type = Ext.typeOf(DesignCode);
		if(type === 'object'){
			
		}else if(type === 'string'){
			
		}
		appParams = {
			panelParams:panelParams,
			southParams:southParams
		};
		
		
		return ClassCode;
	},
	resolveForm:function(DesignCode){
		
	},
	resolveApp:function(DesignCode){
		
	}
};