using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ZhiFang.WebAssist.Common
{
    public class LinqToExcelHelp
    {
        public static void SearchExcelfile(string filePath)
        {
            //var execelfile = new ExcelQueryFactory(filePath);
            //var tsheet = execelfile.Worksheet(0);
            //var query = from p in tsheet
            //            select p;
            //foreach (var row in query)
            //{
            //    foreach (var cell in row)
            //    {
            //        ZhiFang.Common.Log.Log.Debug("Row.Cell="+ cell.Value);
            //    }
            //}
        }
    }
}
